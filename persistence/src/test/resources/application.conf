

db_test {
    dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
    properties = {
        driver = "com.mysql.jdbc.Driver"
        url = "jdbc:mysql://localhost/lightcone_test?characterEncoding=UTF-8"
        user = "root"
        password = "lightcone"
    }
    connectionPool = disabled
}