db {
  default {
    driver = "slick.driver.MySQLDriver$"
    db = {
      url = "jdbc:mysql://127.0.0.1:3306/lightcone?useSSL=false"
      driver = com.mysql.jdbc.Driver
      user = "root"
      password = "111111"
      maxThreads = 5
    }
  }
}