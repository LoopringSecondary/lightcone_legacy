db {
  default {
    profile = "slick.jdbc.MySQLProfile$"
    maxConnections = 100
    minConnections = 20
    numThreads = 20
    maxLifetime = 0
    db = {
      url = "jdbc:mysql://127.0.0.1:3306/lightcone?characterEncoding=UTF-8&useSSL=false"
      driver = com.mysql.jdbc.Driver
      user = "root"
      password = "111111"
    }
  }
  connection-pool {
    parallelism-min = 3
    parallelism-factor = 1.0
    parallelism-max = 5
  }

  dbconfig-dal-token-metadata: ${db.default}
  dbconfig-dal-order: ${db.default}
  dbconfig-dal-trade: ${db.default}
  dbconfig-dal-token-balance: ${db.default}
  dbconfig-dal-block: ${db.default}
  dbconfig-dal-settlement-tx: ${db.default}
  dbconfig-dal-order-status-monitor: ${db.default}
  dbconfig-dal-transaction_record_0: ${db.default}
  dbconfig-dal-transaction_record_1: ${db.default}
}

#TODO:du 和东哥确认是否这么配(TransactionRecordActor.selfConfig)，同步到main
transaction-record_0 {
  db = ${db.dbconfig-dal-transaction_record_0}
}

transaction-record_1 {
  db = ${db.dbconfig-dal-transaction_record_1}
}